<%= turbo_frame_tag "strong-definition" do %>
  <div class="fixed inset-0 bg-black bg-opacity-80 z-50 flex items-center justify-center"
       data-controller="modal"
       data-modal-target="backdrop"
       data-action="click->modal#closeOnOutside keydown@document->modal#handleEscape">

    <div class="bg-gray-800 border-2 border-red-400 rounded-xl p-6 lg:p-8 max-w-2xl w-full mx-4 shadow-2xl"
         onclick="event.stopPropagation()">

      <div class="flex justify-between items-center mb-6">
        <h2 class="text-2xl lg:text-3xl font-bold text-red-400">
          ❌ Strong's No Encontrado
        </h2>

        <%= link_to "✕",
            "#",
            class: "text-gray-400 hover:text-white text-2xl font-bold leading-none",
            title: "Cerrar",
            data: { action: "click->modal#close" } %>
      </div>

      <div class="text-center">
        <p class="text-lg lg:text-xl text-gray-300 mb-6">
          No se encontró la definición de Strong's solicitada.
        </p>
        
        <p class="text-base lg:text-lg text-gray-400 mb-8">
          Es posible que el número de Strong's no exista en nuestra base de datos o que haya un error en la referencia.
        </p>

        <div class="flex justify-center">
          <%= link_to "✓ Entendido",
              "#",
              class: "bg-blue-600 hover:bg-blue-700 text-white font-semibold px-6 lg:px-8 py-3 lg:py-4 rounded-lg text-base lg:text-xl transition-colors duration-200",
              data: { action: "click->modal#close" } %>
        </div>
      </div>
    </div>
  </div>
<% end %>
